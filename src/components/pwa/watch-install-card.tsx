"use client";
import { Download, Share } from "lucide-react";
import { useState } from "react";
import { trackEvent } from "@/lib/client-analytics";
import { usePwaInstall } from "@/components/pwa/pwa-provider";
function telegram(){return typeof window!=="undefined"&&Boolean((window as typeof window&{Telegram?:{WebApp?:unknown}}).Telegram?.WebApp)}
export function WatchInstallCard(){const{event,installed}=usePwaInstall();const[iosHelp,setIosHelp]=useState(false);if(installed||telegram())return null;const ua=typeof navigator!=="undefined"?navigator.userAgent:"";const ios=/iPad|iPhone|iPod/.test(ua)||(typeof navigator!=="undefined"&&navigator.platform==="MacIntel"&&navigator.maxTouchPoints>1);async function install(){trackEvent("pwa_install_clicked");if(event){await event.prompt();const choice=await event.userChoice;trackEvent(choice.outcome==="accepted"?"pwa_install_accepted":"pwa_install_dismissed");}else if(ios){setIosHelp(true);trackEvent("pwa_ios_help_opened");}}if(!event&&!ios)return null;return <section className="rf-section"><div className="overflow-hidden rounded-2xl border border-white/[.08] bg-[linear-gradient(135deg,rgba(229,9,20,.14),rgba(255,255,255,.025))] p-5 sm:p-7"><div className="flex flex-col gap-5 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-2xl font-semibold text-white">Установите REDFILM</h2><p className="mt-2 text-[#a1a1aa]">Быстрый доступ к фильмам и сериалам</p></div><button onClick={install} className="mf-btn mf-btn-primary min-h-12 gap-2"><Download size={18}/>{ios?"Добавить на главный экран":"Установить"}</button></div>{iosHelp?<div className="mt-5 rounded-xl border border-white/10 bg-black/30 p-4 text-sm text-[#d4d4d8]"><div className="flex items-center gap-2 font-semibold text-white"><Share size={16}/>На iPhone или iPad</div><p className="mt-2">Нажмите «Поделиться», выберите «На экран Домой», затем нажмите «Добавить».</p></div>:null}</div></section>}
