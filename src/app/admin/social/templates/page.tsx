import { prisma } from "@/lib/prisma"; export const dynamic="force-dynamic";
export default async function Page(){const rows=await prisma.socialTemplate.findMany({orderBy:{name:"asc"}});return <div className="admin-panel p-5"><h2 className="text-2xl font-bold">Шаблоны рубрик</h2><p className="mt-2 text-neutral-600">Шаблоны хранят системный и пользовательский промпты. Первичные шаблоны добавляются миграцией.</p><div className="mt-4 space-y-3">{rows.map(r=><div key={r.id} className="rounded-xl border p-4"><b>{r.name}</b><div className="text-sm text-neutral-500">{r.category} · {r.isActive?"активен":"выключен"}</div></div>)}</div></div>}
