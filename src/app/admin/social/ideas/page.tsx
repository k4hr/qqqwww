import { prisma } from "@/lib/prisma"; import { generateIdeasAction,researchIdeaAction } from "../actions";
export const dynamic="force-dynamic";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string>>}){const q=await searchParams;const [ideas,movies]=await Promise.all([prisma.socialIdea.findMany({orderBy:[{potentialScore:"desc"},{createdAt:"desc"}],take:100}),prisma.movie.findMany({where:{isPublished:true},select:{id:true,titleRu:true,year:true},orderBy:{views:"desc"},take:300})]);return <div className="space-y-5"><form action={generateIdeasAction} className="admin-panel space-y-3 p-5"><h2 className="text-2xl font-bold">AI-банк идей</h2>{q.error?<div className="text-red-600">{q.error}</div>:null}<select name="movieId" className="w-full rounded-xl border p-3"><option value="">Выбрать фильм</option>{movies.map(m=><option key={m.id} value={m.id}>{m.titleRu} ({m.year})</option>)}</select><input name="title" placeholder="Или произвольная тема" className="w-full rounded-xl border p-3"/><button className="rounded-xl bg-[#e50914] px-5 py-3 font-bold text-white">Создать 12 идей</button></form><div className="admin-panel p-5"><div className="space-y-3">{ideas.map(i=><div key={i.id} className="rounded-xl border p-4"><div className="flex justify-between gap-4"><div><b>{i.topic}</b><div className="text-sm text-neutral-500">{i.category} · потенциал {i.potentialScore?.toFixed(1)||"—"}</div><p className="mt-2">{i.hook}</p></div>{i.status==="NEW"?<form action={researchIdeaAction}><input type="hidden" name="ideaId" value={i.id}/><button className="rounded-lg bg-[#222] px-3 py-2 font-bold text-white">Исследовать и создать черновик</button></form>:<b>{i.status}</b>}</div></div>)}</div></div></div>}
