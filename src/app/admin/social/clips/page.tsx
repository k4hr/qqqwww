import Link from "next/link"; import { prisma } from "@/lib/prisma";
export const dynamic = "force-dynamic";
export default async function Page() { const clips = await prisma.socialPost.findMany({ where: { type: "CLIP" }, include: { media: { include: { mediaAsset: true } } }, orderBy: { createdAt: "desc" }, take: 100 }); return <div className="admin-panel p-5"><div className="flex justify-between"><h2 className="text-2xl font-bold">VK Clips</h2><Link href="/admin/social/clips/new" className="rounded-xl bg-[#e50914] px-4 py-2 font-bold text-white">Загрузить клип</Link></div><div className="mt-5 space-y-3">{clips.map((clip) => <div key={clip.id} className="rounded-xl border p-4"><b>{clip.title}</b><div className="text-sm text-neutral-500">{clip.status} · {clip.scheduledAt?.toLocaleString("ru-RU") || "сейчас"} · {clip.media[0]?.mediaAsset.fileName}</div>{clip.lastError ? <div className="text-red-600">{clip.lastError}</div> : null}</div>)}</div></div> }
