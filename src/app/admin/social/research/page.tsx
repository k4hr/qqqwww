import { prisma } from "@/lib/prisma"; export const dynamic="force-dynamic";
export default async function Page(){const rows=await prisma.socialResearch.findMany({orderBy:{createdAt:"desc"},take:100,include:{_count:{select:{sources:true,facts:true}}}});return <div className="admin-panel p-5"><h2 className="text-2xl font-bold">Исследования и фактчекинг</h2><div className="mt-4 space-y-3">{rows.map(r=><div key={r.id} className="rounded-xl border p-4"><b>{r.topic}</b><div className="text-sm text-neutral-500">{r.status} · источников {r._count.sources} · фактов {r._count.facts}</div><p className="mt-2 line-clamp-3">{r.summary}</p></div>)}</div></div>}
