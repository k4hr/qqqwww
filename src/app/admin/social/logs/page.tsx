import { prisma } from "@/lib/prisma"; export const dynamic="force-dynamic";
export default async function Page(){const logs=await prisma.socialAuditLog.findMany({orderBy:{createdAt:"desc"},take:200});return <div className="admin-panel p-5"><h2 className="text-2xl font-bold">Аудит-лог</h2><div className="mt-4 space-y-2">{logs.map(l=><details key={l.id} className="rounded-xl border p-3"><summary><b>{l.action}</b> · {l.entityType} · {l.createdAt.toLocaleString("ru-RU")}</summary><pre className="mt-3 overflow-auto text-xs">{JSON.stringify({before:l.before,after:l.after,metadata:l.metadata},null,2)}</pre></details>)}</div></div>}
