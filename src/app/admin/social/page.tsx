import Link from "next/link";
import { prisma } from "@/lib/prisma";
export const dynamic = "force-dynamic";
export default async function Page() { const [posts, scheduled, published, failed, media, jobs, ai] = await Promise.all([prisma.socialPost.count(), prisma.socialPost.count({ where: { status: "SCHEDULED" } }), prisma.socialPost.count({ where: { status: "PUBLISHED" } }), prisma.socialPost.count({ where: { status: "FAILED" } }), prisma.socialMediaAsset.count({ where: { status: "READY" } }), prisma.socialPublishJob.count({ where: { status: { in: ["PENDING", "RETRY", "RUNNING", "LEASED"] } } }), prisma.socialAiUsage.aggregate({ _sum: { inputTokens: true, outputTokens: true } })]); const stats = [["Всего публикаций", posts], ["Запланировано", scheduled], ["Опубликовано", published], ["Ошибки", failed], ["Медиа в R2", media], ["Задачи", jobs]]; return <><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{stats.map(([label, value]) => <div key={String(label)} className="admin-panel p-5"><div className="text-sm text-neutral-500">{label}</div><div className="mt-2 text-4xl font-bold text-[#e50914]">{value}</div></div>)}</div><div className="mt-5 grid gap-4 lg:grid-cols-2"><div className="admin-panel p-5"><h2 className="text-xl font-bold">Быстрый старт</h2><div className="mt-4 flex flex-wrap gap-2"><Link className="rounded-xl bg-[#222] px-4 py-3 font-bold text-white" href="/admin/social/posts/new">Создать пост</Link><Link className="rounded-xl bg-[#222] px-4 py-3 font-bold text-white" href="/admin/social/clips/new">Загрузить клип</Link><Link className="rounded-xl border px-4 py-3 font-bold" href="/admin/social/integrations">Подключить VK</Link></div></div><div className="admin-panel p-5"><h2 className="text-xl font-bold">OpenAI</h2><p className="mt-3 text-neutral-600">Токены за всё время: вход {ai._sum.inputTokens || 0}, выход {ai._sum.outputTokens || 0}.</p></div></div></> }
