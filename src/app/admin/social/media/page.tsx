import { prisma } from "@/lib/prisma";
export const dynamic="force-dynamic";
export default async function Page(){const media=await prisma.socialMediaAsset.findMany({where:{status:{not:"DELETED"}},orderBy:{createdAt:"desc"},take:100});return <div className="admin-panel p-5"><h2 className="text-2xl font-bold">Медиатека R2</h2><div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{media.map(m=><div key={m.id} className="rounded-xl border p-3">{m.kind==="IMAGE"?<img src={`/api/admin/social/media/${m.id}/preview`} alt="" className="aspect-video w-full rounded-lg object-cover"/>:<video src={`/api/admin/social/media/${m.id}/preview`} controls preload="metadata" className="aspect-video w-full rounded-lg bg-black"/>}<b className="mt-2 block truncate">{m.fileName}</b><div className="text-xs text-neutral-500">{m.kind} · {m.status} · {m.fileSize?`${Math.round(Number(m.fileSize)/1024/1024)} MB`:"—"}</div><div className="mt-1 truncate text-xs">{m.sourceDomain||m.sourceType}</div></div>)}</div></div>}
